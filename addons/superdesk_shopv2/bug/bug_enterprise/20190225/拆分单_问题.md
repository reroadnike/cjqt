


订单编号 : ME20190222182259686818 京东单号 : 88076506447
|----订单编号:ME20190222182604406222 京东单号:84153647418
|----订单编号:ME20190222182604388620 京东单号:84155229976

delete FROM ims_superdesk_shop_order_goods
where 
    price = 0.00 
    and id in (31849,31850,31851)


订单编号 : ME20190222181357820422 京东单号 : 88076891213
|----订单编号:ME20190222182504687497 京东单号:88076310151
|----订单编号:ME20190222182505811979 京东单号:88075405090

delete FROM ims_superdesk_shop_order_goods
where 
    price = 0.00 
    and id in (31848,31847,31846)


订单编号 : ME20190222162504745491 京东单号 : 88070537734  ---- 21499
|----订单编号:ME20190222163702545456 京东单号:88070713422 ---- 21503
|----订单编号:ME20190222163702884909 京东单号:88071194694 ---- 21504

delete FROM ims_superdesk_shop_order_goods
where 
    price = 0.00 
    and id in (31814,31813)


1550823904

SELECT * FROM `ims_superdesk_shop_order` where createtime = 1550823904

查m
SELECT parentid , createtime FROM `ims_superdesk_shop_order` where expresssn = '88070537734'

SELECT * FROM `ims_superdesk_shop_order` where id = 21499 or parentid = 21499
SELECT * FROM `ims_superdesk_shop_order_goods` WHERE orderid in (21499,21503,21504) ORDER BY orderid asc,id ASC
SELECT * FROM `ims_superdesk_jd_vop_order_submit_order` WHERE order_id in (21499,21503,21504)



SELECT orderid,parent_order_id,goodsid,openid,core_user FROM `ims_superdesk_shop_order_goods` WHERE core_user = 0


SELECT og.orderid,og.parent_order_id,og.goodsid,og.openid,og.core_user,o.core_user,o.source_from 
FROM ims_superdesk_shop_order_goods og 
    left join ims_superdesk_shop_order o on o.id = og.orderid
WHERE o.core_user = 0


-- 可修复
SELECT og.orderid,og.parent_order_id,og.goodsid,og.openid,o.core_user as order_core_user,og.core_user as order_goods_core_user,o.source_from 
FROM ims_superdesk_shop_order_goods og 
left join ims_superdesk_shop_order o on o.id = og.orderid
WHERE o.core_user > 0 and og.core_user = 0






delete FROM ims_superdesk_shop_order_goods
where 
    price = 0.00 
    and orderid in ()





正在显示第 0 - 24 行 (共 277 行, 查询花费 0.0027 秒。)

SELECT 
	o.core_user as order_core_user,og.core_user as order_goods_core_user,
    o.openid as order_openid,og.openid as order_goods_openid,m.openid as member_openid,
	og.orderid,og.parent_order_id,og.goodsid,o.source_from ,
    m.realname
FROM ims_superdesk_shop_order_goods og 
left join ims_superdesk_shop_order o on o.id = og.orderid
left join ims_superdesk_shop_member m on m.core_user = o.core_user
WHERE o.core_user > 0 and og.core_user = 0


283

正在显示第 150 - 160 行 (共 161 行, 查询花费 0.1896 秒。)

SELECT 
	o.core_user as order_core_user,og.core_user as order_goods_core_user,
    o.openid as order_openid,og.openid as order_goods_openid,m.openid as member_openid,
	og.orderid,og.parent_order_id,og.goodsid,o.source_from ,
    m.realname
FROM ims_superdesk_shop_order_goods og 
left join ims_superdesk_shop_order o on o.id = og.orderid
left join ims_superdesk_shop_member m on m.core_user = o.core_user
WHERE o.core_user > 0 and og.core_user = 0 and og.goodsid =0




正在显示第 0 - 24 行 (共 122 行, 查询花费 0.0995 秒。)

SELECT 
	o.core_user as order_core_user,og.core_user as order_goods_core_user,
    o.openid as order_openid,og.openid as order_goods_openid,m.openid as member_openid,
	og.orderid,og.parent_order_id,og.goodsid,o.source_from ,
    m.realname
FROM ims_superdesk_shop_order_goods og 
left join ims_superdesk_shop_order o on o.id = og.orderid
left join ims_superdesk_shop_member m on m.core_user = o.core_user
WHERE o.core_user > 0 and og.core_user = 0 and og.goodsid >0



0

SELECT 
	o.id,og.id,o.expresscom,o.expresssn,
	o.status,
    FROM_UNIXTIME(o.createtime),
	o.core_user as order_core_user,og.core_user as order_goods_core_user,
    o.openid as order_openid,og.openid as order_goods_openid,m.openid as member_openid,
	og.orderid,og.parent_order_id,og.goodsid,og.price,og.total,o.source_from ,
    m.realname
FROM ims_superdesk_shop_order_goods og 
left join ims_superdesk_shop_order o on o.id = og.orderid
left join ims_superdesk_shop_member m on m.core_user = o.core_user
WHERE o.core_user > 0 and og.core_user = 0 and og.goodsid =0 and o.createtime > 1550764800
ORDER BY o.id



116
SELECT 
	o.id,og.id,o.expresscom,o.expresssn,
	o.status,
    FROM_UNIXTIME(o.createtime),
	o.core_user as order_core_user,og.core_user as order_goods_core_user,
    o.openid as order_openid,og.openid as order_goods_openid,m.openid as member_openid,
	og.orderid,og.parent_order_id,og.goodsid,og.price,og.total,o.source_from ,
    m.realname
FROM ims_superdesk_shop_order_goods og 
left join ims_superdesk_shop_order o on o.id = og.orderid
left join ims_superdesk_shop_member m on m.core_user = o.core_user
WHERE o.core_user > 0 and og.core_user = 0 and og.goodsid >0 and o.createtime > 1550764800
ORDER BY o.id


101
SELECT 
	o.id,og.id,o.expresscom,o.expresssn,
	o.status,
    FROM_UNIXTIME(o.createtime),
	o.core_user as order_core_user,og.core_user as order_goods_core_user,
    o.openid as order_openid,og.openid as order_goods_openid,m.openid as member_openid,
	og.orderid,og.parent_order_id,og.goodsid,og.price,og.total,o.source_from ,
    m.realname
FROM ims_superdesk_shop_order_goods og 
left join ims_superdesk_shop_order o on o.id = og.orderid
left join ims_superdesk_shop_member m on m.core_user = o.core_user
WHERE o.core_user > 0 and og.core_user > 0 and og.goodsid >0 and o.createtime > 1550764800
ORDER BY o.id

217
SELECT 
	o.id,og.id,o.expresscom,o.expresssn,
	o.status,
    FROM_UNIXTIME(o.createtime),
	o.core_user as order_core_user,og.core_user as order_goods_core_user,
    o.openid as order_openid,og.openid as order_goods_openid,m.openid as member_openid,
	og.orderid,og.parent_order_id,og.goodsid,og.price,og.total,o.source_from ,
    m.realname
FROM ims_superdesk_shop_order_goods og 
left join ims_superdesk_shop_order o on o.id = og.orderid
left join ims_superdesk_shop_member m on m.core_user = o.core_user
WHERE o.core_user > 0  and og.goodsid >0 and o.createtime > 1550764800
ORDER BY o.id















